apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: devops-task-manager
  labels:
    app: user-service
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
